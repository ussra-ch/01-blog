services:
    postgres:
        container_name: mydb
        image: postgres:16-alpine
        env_file:
            - db.env
        volumes:
            - postgres-data:/var/lib/postgresql/data
        ports:
            - 5431:5432

volumes:
    postgres-data:
